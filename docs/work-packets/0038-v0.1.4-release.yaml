id: CFO-0038
title: Publish v0.1.4 documentation hygiene patch
status: done
owner: lead
role: release
priority: p1
objective: Prepare and publish the v0.1.4 GitHub release for public documentation hygiene updates.
paths:
  - CHANGELOG.md
  - package.json
  - package-lock.json
  - docs/RELEASE_READINESS.md
  - docs/TASKS.md
  - docs/MAINTENANCE_AUDIT.md
  - docs/work-packets/0038-v0.1.4-release.yaml
concerns:
  - concern:release-v0.1.4
dependencies:
  - CFO-0037
non_goals:
  - Do not edit `.env`.
  - Do not publish to npm.
  - Do not change runtime behavior.
acceptance:
  - Package metadata is bumped to `0.1.4`.
  - Changelog has a dated `0.1.4` entry describing the public documentation hygiene patch.
  - Release readiness references release notes through `v0.1.4`.
  - Maintenance audit and task tracker record the release packet.
  - Full release gate passes before tag and GitHub release publication.
verification:
  required:
    - npm run check
    - git diff --check
    - targeted public-risk string scan
  completed:
    - npm run check
    - git diff --check
    - targeted public-risk string scan
  evidence:
    - Full release gate passed with setup validation, typecheck, build, 102 tests, work-packet validation, release readiness, high-severity npm audit, and secret scan.
    - Targeted public-risk scan returned no unexpected matches; only the intentional `AGENTS.md` publication guardrail lines remain.
    - Release metadata commit is `1dee71c`.
handoff:
  expected:
    - Report commit SHA, tag, GitHub release URL, verification, and remaining operator actions.
  completed:
    - Prepared release metadata, packet, task tracker, and maintenance audit for `v0.1.4`.
  next:
    - Push `main`, push tag `v0.1.4`, and publish the GitHub release after the clean gate.
risks:
  - A docs-only patch can still create public confusion if release notes over-explain internal maintenance context.
maintenance_audit:
  assigned_at: "2026-04-15"
  completed_at: "2026-04-15"
  reviewed_by: lead
  checks:
    - npm run check
    - git diff --check
    - targeted public-risk string scan
  commit: 1dee71c
  notes:
    - Created after maintainer authorization to publish the documentation hygiene cleanup.
    - Release publication proceeded under maintainer authorization.
